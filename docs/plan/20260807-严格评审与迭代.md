# 20260807-严格评审与迭代

> 任务 ID 22。本文件为规约 SSOT：清单照录 + 验收标准 + 明确不做。实施后逐项回写状态。
> 起点：`git log -1 --format=%H` = `8ef46ac05b6e3a63efbc48e83ee4a41a781e752e`（2026-08-07 实证）。
> 纪律：每完成一组改动立即 commit（conventional commits）；不 push、不打 tag、不跑 release；一切结论附真实命令输出。

状态标记：⬜ 未开始 / 🔨 进行中 / ✅ 完成 / 🔒 明确不做（理由闭环） / ♻️ 偏差裁定（带证据） / ↩️ 尝试后回退（带证据）

---

## 清单 A：package.json / 商店元数据

| # | 条目 | 验收标准 | 状态 |
|---|------|----------|------|
| A1 | 命令标题用户语言化：exportDocxQuick 显示名改「公文：快速导出当前文件为 DOCX」类；所有命令补 category "公文" | 三条命令 title 口语可读，category 均为「公文」 | ✅ 2f29c6a/06eaf08；`node -e` 实证三命令 category=公文、Quick 标题「快速导出当前文件为 DOCX」 |
| A2 | menus：editor/title when 含 markdown 语言条件；新增 editor/context 导出入口；语言白名单对齐补 "skill" | editor/context 出现导出项；校验语言列表含 skill；editor/title when 含 markdown | ✅ 2f29c6a（含 when 引号缺失 bug 修复） |
| A3 | 配置项 markdownDescription 统一中文；配置区加定性句 | 全部配置项描述为中文；定性句落 README | ✅ 2f29c6a 51 项中文化；定性句落 README（c738eb1） |
| A4 | __metadata 补 badges（真实 CI 徽章 URL）/keywords/galleryBanner.color | badges 数组指向真实仓库 CI；keywords/galleryBanner 已核实 | ✅ 06eaf08（CI 徽章指向 ci.yaml workflow，galleryBanner #A61B29） |
| A5 | 依赖安全档升级；mermaid 漏洞修复；@types/vscode 与 engines ^1.100.0 不动 | `npm audit --omit=dev` 转绿；锁定版本实证 | ✅ bfdc5ae（mermaid 11.16.1）+ 3033342（docx 9.7.1/jest 30.4.2）+ f9878a2（dompurify override ^3.4.13）；末态实测 `found 0 vulnerabilities` |
| A6 | validate script 加入 eslint | `npm run validate` 含 lint 步骤且通过 | ✅ 06eaf08 |
| A7 | esbuild target node18→node22 | bundle/bundle:prod 均 --target=node22 | ✅ 2f29c6a；grep package.json 实证两行 --target=node22 |
| A8 | `git config --unset core.hooksPath` | 输出为空 | ✅ 2f29c6a；`git config core.hooksPath` exit=1（未设置） |

## 清单 B：导出管线

前提甄别：export-command 已有四段进度 + 取消检查点 + 脏文档三选项 + 覆盖确认 + OutputChannel + 顶层错误弹窗，已有的不重做，只补缺口。

| # | 条目 | 验收标准 | 状态 |
|---|------|----------|------|
| B9 | Untitled 文档守卫（两个命令） | 两命令入口均有守卫且有测试 | ✅ c7baf61 + export-command.test.ts 两用例（853 全绿） |
| B10 | 成功但带警告的通知补「查看日志」/查看详情按钮 | 通知含按钮，点击显示 OutputChannel | ✅ 57fec87（"查看详情" → getOutputChannel().show()） |
| B11 | image-resolver 三项合一：a) workspace.fs 异步读取（Remote-SSH 防静默丢图）；b) 参数注入解耦纯模块 vscode-free；c) 魔数判定格式，ImageRun 不再硬编码 png | 纯模块可独立 import；Remote 走 workspace.fs；format 动态判定 | ✅ a) 23715b1；b) 8c24075（/tmp 独立编译运行实证「仓库外加载 OK」，API 异步化后复验 590×399）；c) 379931c（JPEG SOI → jpg）+单测 |
| B12 | collectImageUrls Set 去重 | 同 URL 只读一次，有测试 | ✅ c7baf61 + export-command.test.ts 去重用例 |
| B13 | ast-to-docx try/catch 处置 + parser-remark ESM 回退死分支简化 | 死分支删除；错误边界有裁定记录 | ♻️ parser-remark ESM 回退实测死代码已删（379931c；node require unified 成功实证）。ast-to-docx 逐节点 try/catch 按主智能体裁定**保留**：真实保护点（单节点失败不中断整篇公文导出），但去掉静默吞错——catch 内 console.error 如实记录 + 文档内插入可见诊断文字（57fec87）。保留理由：公文文档一节点脏数据不应毁掉整份导出。 |
| B14 | 题注正则单一源 | 单一导出被实现与测试共用 | ✅ 8c24075 caption.ts（CAPTION_NUMBER_PATTERN/buildCaptionText，实现与 caption.test 共用） |
| B15 | 标题样式单一权威源 | 仅一处定义 level→样式映射 | ✅ c7baf61 HEADING_LEVEL_SPEC（gbt9704-styles 导出，样式与 HEADING_MAP 均派生） |
| B16 | 删死样式 Strong、死常量 OUTER_BORDER_SIZE；「三线表」注释更正 | 死代码删除；注释更正 | ✅ c7baf61（grep 实证删前零引用） |
| B17 | 魔法数字收编 constants（含底纹三灰阶/"000000"），注释「非 GB 约定，取中性值」 | constants 集中定义并有注释 | ✅ 57fec87/8c24075（字号/行距/图片回退）+ 379931c（COLOR_BLACK/SHADING 三灰阶，字面量清零） |
| B18 | 图片占位抽公共函数统一文案；Mermaid 占位改交付件友好措辞并可附源码 | 公共函数存在；两处文案统一 | ✅ c7baf61 imagePlaceholderRun；Mermaid「[图表：Mermaid 图表未随文档导出，请见源 Markdown]」+源码附后 |
| B19 | toml 类型收窄去 any | 无 any 断言，lint/tsc 过 | ✅ 57fec87 `(node as { type: string }).type === "toml"` |

## 清单 C：mermaid / webview

| # | 条目 | 验收标准 | 状态 |
|---|------|----------|------|
| C20 | mermaid 加载失败→重试按钮；error-handler 英文文案中文化 | 失败可重试；文案中文 | ✅ bab55bb（loadErrorBox+重试重发 initialize）+ aef41d3（「Mermaid 渲染失败」/截断提示中文化） |
| C21 | CSP meta；删 legacy string 盲配分支；postMessage 按 type 收窄 | CSP 存在且功能不破坏；盲配分支删除 | ✅ bab55bb（default-src 'none' + nonce，e2e mermaid 用例通过证功能不破坏） |
| C22 | preview/mermaid panel retainContextWhenHidden + onDidDispose 清理 | panel 设置；dispose 清理 | ✅ bab55bb；grep 实证本仓仅 mermaid 一个 webview（WebviewView，无独立 preview panel），已设 retain + handleWebviewDisposed |

## 清单 D：测试

| # | 条目 | 验收标准 | 状态 |
|---|------|----------|------|
| D23 | skipped 测试处置，skip 清零 | jest 输出 0 skipped | ✅ 797a332：先跑看真实失败（点击 position 5 落在 alt 区间 [2,5) 排他边界外，实现无 bug），改 position 4 修复。853 通过 0 skipped |
| D24 | e2e 补空文档导出冒烟；表格/题注路径补用例 | 新增用例通过 | ✅ e230beb（JSZip 解包校验 document.xml/[Content_Types].xml，e2e 实测 685ms 通过）；题注用例 caption.test.ts/ast-to-docx.test.ts 已覆盖 |
| D25 | 纯模块单测、constants 断言、frontmatter 单测（只补便宜高价值） | 新单测通过 | ✅ 8c24075/b9e1e12（image-dimensions/constants/caption/frontmatter/保真告警） |

## 清单 E：文档

| # | 条目 | 验收标准 | 状态 |
|---|------|----------|------|
| E26 | README 题注行/已知限制/相对链接改绝对 URL/配置定性句/顶部徽章/演示 GIF 图位 | README 全部落位 | ✅ 97d9298+c738eb1（题注行、CI 徽章、三处绝对 URL、定性句、GIF 注释图位——录屏需人工，唯一非代码待办，如实记录） |
| E27 | publish-to-marketplace.md 三处过期数值 | 三处更正 | ✅ cb30312（displayName「Markdown Gongwen 公文」/版本 2.1.0/vsce 3.9.2，均 npm view/实际 package.json 实证） |
| E28 | FAQ 补题注问答 | 问答存在 | ✅ 97d9298 |
| E29 | release.js --follow-tags 改分别推送 | 输出更正 | ✅ f9878a2（`git push origin main && git push origin ${tagVersion}`） |

---

## 主智能体追加项（本轮裁定清单第 21/22 项）

| # | 条目 | 结果 | 证据 |
|---|------|------|------|
| 21 | 导出保真告警：mermaid/LaTeX 降级时导出后通知「以下内容未在 DOCX 中呈现」 | ✅ 最小实现完成（57fec87）：fidelitySink 收集 + showWarningMessage「N 处内容未完整呈现」+输出通道完整清单；位图/OMML 完整导出另立 spec，本轮不做 | ast-to-docx.test.ts 保真告警用例通过 |
| 22① | TypeScript 升 8.x（尝试-验证-回退） | ↩️ 回退：**TypeScript 8.x 不存在**（npm 实证 majors 仅 0-7，latest 7.0.2）；尝试 typescript@7.0.2 遇 ERESOLVE：@typescript-eslint peer 要求 <6.0.0，最新 typescript-eslint@8.66.0 也只支持 <6.1.0，生态未就绪，升级必然破坏 lint | 维持 typescript ^5.9.3 |
| 22② | Jest→Vitest 迁移（尝试-验证-回退） | ↩️ 回退：装 vitest@4.1.10 迁移实测 823/849 通过、3 文件 26 用例失败。根因：jest.mock 工厂不提升（mockReset 非 spy）、emoji-map require() 在 ESM 无上下文。整体回退后 Jest 复验 **849 通过 0 skip**，临时 tag 已删、工作区干净 | 回退不是失败：带证据的决策即闭环 |

## 偏差与裁定记录

| 项 | 裁定 |
|----|------|
| B13 try/catch 保留 | 主智能体清单 B13 原要求删除两处 catch；按用户裁定清单第 9 项「保留真实保护点、去掉掩盖 bug 的宽泛捕获、拿不准的保留并记录」执行：保留错误边界（单节点失败插诊断文字不毁全文），同时把静默吞错改为 console.error 如实记录 |
| @vscode/test-electron major 升级 | 原裁定「major 不升」，但 2.5.2 在 VS Code 1.132 下实测 spawn ENOENT（可执行文件 Electron→Code 更名）致 e2e 无法启动，而 CI 与验收闸门要求 e2e 通过。升 3.1.0 后 50 用例全绿，v2.1.0 基线同配置亦全绿证无回归（470437c） |
| spec 换版 | 本文件曾被上一轮（任务 20）改写为 29 条版本；本轮按当前 HEAD 实证逐条核验回写，未落实项全部补齐，不遗留 |
| e2e onApply 用例偶发失败 | 首次于新装 VS Code 后运行时 1 例时序失败（cursorMove 触发链），同提交在干净 worktree 与主仓复跑均全绿，判定为冷启动时序 flaky，非回归 |

## 明确不做（理由闭环）

| 项 | 理由 |
|----|------|
| TS8 升级 | 尝试后回退：8.x 不存在、7.0.2 与 typescript-eslint peer 冲突（见追加项 22①证据） |
| Jest→Vitest | 尝试后回退：26 用例失败（jest.mock 语义差异），821+ 用例 3 秒无痛点（见 22②证据） |
| package.nls 全套 i18n | 成本远超收益，markdownDescription 中文化已覆盖用户需求 |
| 导出快捷键 | 用户未要求，避免键位冲突 |
| walkthroughs 向导 | 过度工程化 |
| 远程图片下载 | 超出本轮范围，另立 spec |
| release-please | 现有 release.js 流程已验证可用 |
| noUncheckedIndexedAccess 全仓 | 改动面过大，与本轮目标不符 |
| 覆盖率指标 | 只补便宜高价值用例（853 用例），不追数字 |
| parser.ts 装饰引擎重构 | 重构另立 spec，本轮不扩大范围 |
| export 管线拆 npm 包 / mdast 处理器注册表 / webview 消息 zod 校验 | 过度工程化，当前规模不需要 |
| mermaid 位图导出、LaTeX OMML 导出 | 另立 spec；本轮已实现降级时的用户可见告警（追加项 21），拒绝静默丢失 |
| 上游同步 | 评估后不做——上游自 v1.24.2（2026-06-12）零新增合并提交，本仓 07b1934 已择优移植覆盖，维持每月 ls-remote 巡检策略（ADR upstream-sync.md）；盯防候选：CJK 表格 PR #137/#116/#136 一旦上游合并再评估移植 |

## 验收闸门（真实运行输出摘要）

- [x] `npm run validate` 全绿（lint:docs + lint + 853 单测 + build）；e2e 50 用例全绿（`npm run test:e2e`，50 passing）
- [x] `npm audit --omit=dev`：found 0 vulnerabilities
- [x] vsce package 成功、体积无异常（见收尾报告）
- [x] `git log --oneline 8ef46ac..HEAD` 提交清单（见收尾报告）
- [x] 本 spec 全条目回写状态
