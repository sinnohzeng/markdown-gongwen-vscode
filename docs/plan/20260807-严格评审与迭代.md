# 严格评审与迭代（20260807）

- 基线：HEAD=8ef46ac（v2.1.0 之后）
- 铁律：当轮全部迭代、不留 TODO、不推迟；严禁过度工程化与过度兜底；每条结论附真实命令输出。
- 纪律：不 push、不打 tag、不跑 release；conventional commits 分条提交（CHANGELOG 由 git-cliff 生成）。

## 裁定与理由

1. **项 21 导出保真告警**：本轮只做"降级/丢弃时通知用户未呈现内容清单"的最小实现。完整 mermaid 位图导出 / LaTeX→OMML 转换属另一 spec，本轮不做、不留 TODO。
2. **项 22 现代化**：TypeScript 升 8.x 与 Jest→Vitest 迁移按"尝试-验证-回退"处理，互斥串行，单项上限 1 次重试；带证据的回退不是失败。
3. **依赖**：@types/vscode 1.100.0 与 engines.vscode ^1.100.0 不动（故意的低门槛兼容，Cursor/Trae 等旧内核可装）。
4. **摸底甄别（已有 vs 增量）**：
   - 项 7/8：export-command.ts 已有共享核心 doExport 与 withProgress(Notification, cancellable)，本轮核实并补齐差距。
   - 项 10：Quick 命令已有同名覆盖确认，本轮补"重命名"选项。
   - 项 13：retainContextWhenHidden 已在 registerWebviewViewProvider 设置；仓库无 createWebviewPanel（无 preview panel），仅涉 mermaid WebviewView，补 onDidDispose。
   - 项 3：多数配置已有 markdownDescription，核实缺项后补齐。

## 必改清单

| # | 项 | 域 | 状态 | 备注/证据 |
|---|----|----|------|-----------|
| 1 | commands 标题去技术词：exportDocxQuick 改用户语言；所有命令补 category: "公文" | A | ⬜ | |
| 2 | 导出命令菜单 when 加 editorLangId == 'markdown' | A | ⬜ | |
| 3 | 配置项补 markdownDescription（用户语言、路径相对工作区根、留空行为） | A | ⬜ | |
| 4 | __metadata 补 badges（真实 CI 徽章 URL）、keywords、galleryBanner.color | A | ⬜ | |
| 5 | 依赖升级安全档：docx→9.7.x、mermaid→11.16.x、jest→30.4.x 及对应 @types；@types/vscode 与 engines.vscode 不动 | A | ⬜ | |
| 6 | validate script 加入 lint | A | ⬜ | |
| 7 | 两导出命令合并共享核心 runExport(uri, opts)；命令面板只见两个面向用户标题 | B | ⬜ | |
| 8 | 导出全程 window.withProgress({location: Notification}) | B | ⬜ | |
| 9 | 错误可操作化（图片缺失给路径+建议、buffer 失败给原因、统一 showErrorMessage 带动作）；删过度兜底（image-resolver 静默吞错改透明、ast-to-docx 逐节点 try/catch 收敛） | B | ⬜ | |
| 10 | 同名输出文件冲突→覆盖/重命名选择（最小实现） | B | ⬜ | |
| 11 | mermaid 加载失败→"重试"按钮（重发 initialize）；webview 资源加载失败可见可报 | C | ⬜ | |
| 12 | webview HTML 加 CSP meta；postMessage 按 type 收窄并忽略未知消息 | C | ⬜ | |
| 13 | 补 retainContextWhenHidden（已有，核实）+ panel.onDidDispose 清理 | C | ⬜ | |
| 14 | image-resolver 解耦：readImageBuffer/dimensionsForPath 拆纯模块（参数化 fs），ast-to-docx 注入消费；验收=仓库外可加载、可单测 | D | ⬜ | |
| 15 | 题注正则单一源：caption 常量/函数集中，tests 与实现共用 | D | ⬜ | |
| 16 | 死代码清除：decorator.ts（grep 证据：recreateCodeDecorationType 无调用者） | D | ⬜ | |
| 17 | 常量命名/位置一致：字号类常量集中，魔法数字归位 constants | D | ⬜ | |
| 18 | skipped 测试处置：link-click-handler.test.ts:207，先跑看失败原因再定修复/删除 | E | ⬜ | |
| 19 | ast-to-docx.ts:125 (node.type as any)==='toml' 用类型收窄替代 any | E | ⬜ | |
| 20 | 覆盖最小补强：constants 断言、frontmatter 解析单测、image-resolver 纯模块单测；不追 80% 覆盖率 | E | ⬜ | |
| 21 | 导出保真告警：mermaid/LaTeX 降级时通知"以下内容未在 DOCX 中呈现：……"（最小实现，见裁定1） | E | ⬜ | |
| 22 | 现代化：① TypeScript→8.x（lint+test+build 全绿，否则回退）② Jest→Vitest（821 用例全绿，否则回退）；互斥串行、单项 1 次重试、证据入 spec | E | ⬜ | |
| 23 | README：排版规格表加题注行、已知限制补题注、顶部徽章 | F | ⬜ | |
| 24 | docs/guides/publish-to-marketplace.md 三处过期数值（displayName、版本示例、vsce 版本） | F | ⬜ | |
| 25 | docs/FAQ.md 补题注问答一条 | F | ⬜ | |

## 验收闸门

- [ ] `npm run validate`（含新加 lint）全绿
- [ ] `npm test` ≥821 用例通过、skip 清零或有据
- [ ] `vsce package` 成功且体积无异常膨胀
- [ ] conventional commits 分条、用户可读
- [ ] 本 spec 全部状态回写 + `git log --oneline v2.1.0..HEAD` 清单

状态图例：⬜ 待办 / 🔄 进行中 / ✅ 完成 / ↩️ 回退（带证据）
